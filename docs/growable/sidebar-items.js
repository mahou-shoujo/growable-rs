initSidebarItems({"enum":[["Growable","A chunk of heap memory that can be assigned to a struct or a trait object. Until assigned to some data it behaves similarly to a Box<[u8; N]>, it can be cloned and would be dropped if leaves the scope."]],"struct":[["Reusable","Growable with some data assigned to it. It behaves just like default Box does (so it WILL free memory on drop) but also could be freed manually, fetching Growable back."]]});